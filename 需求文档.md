# WIMO-interview · 面试录音复盘助手（Interview Recap Assistant）需求文档

| 项目 | 内容 |
| --- | --- |
| 版本 | v0.1（草案） |
| 日期 | 2026-08-12 |
| 状态 | 待评审 |
| 产品形态 | 纯前端 Web 应用，无后端；后续可用 Pake 打包为桌面应用 |

---

## 1. 项目概述

### 1.1 背景与目标

用户在面试后希望复盘自己的表现。本应用让用户上传面试录音（音频文件），自动完成两件事：

1. **音频转文字**：调用 AI 语音识别 API，将录音转成逐字稿；
2. **AI 总结复盘**：将逐字稿交给 AI 总结，生成多个维度的复盘内容（面试问题、薄弱点、亮点、改进建议、评分等）。

结果以**多个独立窗口（卡片）** 展示，每个窗口内容为 **Markdown 格式**，并带**一键复制按钮**，方便用户粘贴到自己的笔记工具（Notion、Typora、语雀等）。

### 1.2 核心流程

```
上传音频 → 音频转文字(STT) → AI 总结 → 多窗口展示（Markdown + 复制）
```

### 1.3 产品形态与部署

- **纯前端单页应用（SPA）**，无后端服务，无需部署服务器；
- 可本地运行（`npm run dev`），也可构建后部署到任意静态托管（GitHub Pages / Vercel / 本地静态服务器）；
- 后续通过 **Pake**（Rust/Tauri）将构建产物打包为 macOS / Windows / Linux 桌面应用；
- 因此**不得依赖 Node.js 后端能力**，所有逻辑必须在浏览器内完成。

### 1.4 目标用户

- 求职者：面试后快速复盘，发现薄弱点，为下一次面试做准备；
- 个人自用为主，单用户，无需账号体系。

---

## 2. 功能需求

### FR-1 音频上传

| 编号 | 需求 |
| --- | --- |
| FR-1.1 | 支持点击选择文件与拖拽上传两种方式 |
| FR-1.2 | 支持格式：mp3、wav、m4a、flac、ogg、webm、aac、opus |
| FR-1.3 | 上传前校验文件大小，**超过配置的上限（默认 25MB）时不再拦截，改为警告提示**（不同服务商转写限制不同，如 OpenAI/Groq 25MB、MiMo 等对话式接口约 7.5MB，上限可在设置页调整）；超限文件由应用自动分片转写（见 FR-2.6） |
| FR-1.4 | 可选：上传页允许填写「面试背景信息」（应聘职位 / 公司 / 面试轮次 / 备注），随转写文本一起发给总结模型，使总结更精准（不填也可用） |

### FR-2 音频转文字（STT）

| 编号 | 需求 |
| --- | --- |
| FR-2.1 | 调用**OpenAI 兼容的语音转文字接口**，支持两种协议：Whisper 风格上传接口（`POST {baseURL}/audio/transcriptions`，如 `whisper-1`）与对话式音频输入接口（`POST {baseURL}/chat/completions`，音频 base64 放入 `input_audio`，如小米 `mimo-v2.5-asr`），通过设置页「接口类型」切换 |
| FR-2.2 | 支持在设置中配置：baseURL、API Key、模型名、识别语言（如 zh / en / 自动） |
| FR-2.3 | 转写失败时展示明确错误信息（鉴权失败 / 网络错误 / 超时），并允许重试 |
| FR-2.4 | 转写完成后，**逐字稿在结果页可见**（FR-4 的「完整逐字稿」窗口），并可继续触发 AI 总结 |
| FR-2.5 | 转写结果缓存在本地（localStorage），刷新页面不丢失（仅文本，不存音频） |
| FR-2.6 | **长音频自动分片**：文件超过「最大文件大小」配置时，浏览器端自动解码（AudioContext）→ 重采样 16kHz 单声道 → 按 180 秒切片 → 逐段调用转写接口 → 合并结果；处理页显示「正在转写第 N/M 段」进度；浏览器无法解码的格式（或解码失败）给出明确错误提示 |

### FR-3 AI 总结

| 编号 | 需求 |
| --- | --- |
| FR-3.1 | 调用**OpenAI 兼容的 Chat Completions 接口**（`POST {baseURL}/chat/completions`） |
| FR-3.2 | 支持在设置中配置：baseURL、API Key、模型名、temperature、max_tokens、自定义 System Prompt |
| FR-3.3 | **一次调用**生成完整复盘内容，输出**固定结构的 Markdown**（按约定的一级标题 `#` 分隔各个窗口内容），前端按标题切分渲染到各窗口（见 §4） |
| FR-3.4 | 请求体携带面试背景信息（如有填写，见 FR-1.4） |
| FR-3.5 | 长文本处理：逐字稿过长时，在请求前按字符数阈值截断并提示（V1.2 再做分段总结） |
| FR-3.6 | 总结失败时展示明确错误信息并允许重试 |

### FR-4 结果展示（核心）

| 编号 | 需求 |
| --- | --- |
| FR-4.1 | 结果页由**多个独立窗口（卡片）** 组成，每个窗口有标题栏 + 内容区 + 操作按钮 |
| FR-4.2 | 每个窗口内容**以 Markdown 渲染展示**（标题、列表、加粗、表格、代码块等） |
| FR-4.3 | 每个窗口标题栏带**「复制」按钮**：复制该窗口的 **Markdown 原文**（非渲染后纯文本），保证粘贴到其他笔记工具后仍是结构化内容 |
| FR-4.4 | 复制成功给出轻量反馈（按钮变为「已复制」/ Toast） |
| FR-4.5 | 可选：提供「复制纯文本」次级按钮（长按/下拉），以及「一键导出全部窗口为 .md 文件」（Blob 下载） |
| FR-4.6 | 各窗口内容为空时显示占位文案（如"未生成"），不显示空卡片 |

**窗口清单（9 个）**：

| # | 窗口名称 | 内容说明 |
| --- | --- | --- |
| 1 | 面试问题总结 | 面试官问了哪些问题，按主题/轮次分类整理（技术、项目经历、行为问题、开放题…），逐条列出 |
| 2 | 薄弱/错误总结 | 回答中的薄弱点与错误：逻辑不清、答非所问、知识盲区、技术概念说错、数据/事实有误等，每条标注原文依据 |
| 3 | 表现亮点 | 回答得当的地方：结构清晰、举例具体、追问应对好等，同样标注依据 |
| 4 | 改进建议 | 针对薄弱点的**具体可执行建议**（不是空话），例如"XX 问题应先用 STAR 框架回答""补充 XX 知识点" |
| 5 | 综合评分 | 分维度评分（如：逻辑表达 / 技术深度 / 项目阐述 / 沟通应变，各 10 分）+ 总分 + 一段总结评语（Markdown 表格实现） |
| 6 | 沟通表达分析 | 语速、停顿、口头禅（"嗯/啊/那个"）、语气词频率、是否被打断、音量等（基于逐字稿可推断的部分；受转写质量限制时注明局限） |
| 7 | 提问模式与追问分析 | 面试官的提问风格：是否深挖、追问方向、高频考察点，帮助理解面试官关注什么 |
| 8 | 下次准备清单 | 针对本次面试生成的准备清单（Checklist），如"复习 XX""准备 2 个失败案例"，可直接照着准备下次面试 |
| 9 | 完整逐字稿 | 转写全文，尽量区分「面试官 / 候选人」说话人（Whisper 不支持说话人分离，由总结模型根据上下文推断并标注，能力有限时注明） |

> 窗口 1、2、3、4 为**必须**；5–9 为**建议**（实现时按「一次 prompt 全部生成」设计，前端只负责切分渲染，因此多几个窗口不增加调用成本）。

### FR-5 设置页面

| 编号 | 需求 |
| --- | --- |
| FR-5.1 | 设置页包含**两组相互独立的配置**：STT（音频转文字）与 总结（文字总结），各自可配置不同的服务商/API Key/模型 |
| FR-5.2 | **STT 配置项**：接口类型（上传转写 / 对话式音频输入）、服务商（预设 OpenAI、Groq、SiliconFlow、MiMo、DeepSeek、自定义）、Base URL、API Key、模型名（如 `whisper-1` / `mimo-v2.5-asr`）、识别语言、最大文件大小（MB） |
| FR-5.3 | **总结配置项**：服务商（同 STT 列表，可独立选择）、Base URL、API Key、模型名、temperature（默认 0.3）、max_tokens、自定义 System Prompt（默认提供一套复盘专用提示词，可编辑） |
| FR-5.4 | 两组配置各自提供「测试连接」按钮：STT 组用小段示例音频试转写；总结组发一条测试消息验证连通性，返回成功/失败及原因 |
| FR-5.5 | 配置**仅保存在浏览器 localStorage**，不上传任何服务器 |
| FR-5.6 | 可选：配置支持「导出 / 导入 JSON」，方便换浏览器/设备迁移 |
| FR-5.7 | 页面显著位置给出安全提示：API Key 只存本地，请勿将页面分享给他人（Key 随浏览器数据可见） |
| FR-5.8 | 未配置 API Key 时，上传页给出引导提示并跳转设置页 |

### FR-6 历史记录（V1.1，可选）

- 本地保存历史复盘列表：时间、面试背景信息、逐字稿、各窗口内容；
- 支持查看历史、删除；不存音频文件（体积过大，localStorage 仅存文本）。

---

## 3. 非功能需求

| 类别 | 要求 |
| --- | --- |
| 隐私 | 音频与文本**仅发送至用户配置的 AI API**，应用本身无任何后端、无数据上报；隐私说明写入设置页 |
| 错误处理 | 覆盖：未配置 Key / 鉴权失败（401）/ 网络错误 / 超时（建议 120s）/ 文件过大 / 转写结果为空，均展示可理解的错误信息与重试入口 |
| 进度反馈 | 处理流程分三步展示进度：① 上传中 → ② 转写中 → ③ 总结中，各步显示状态与耗时；转写、总结步骤均可取消或重试 |
| 安全性 | Markdown 渲染必须做 XSS 过滤（不直接 `dangerouslySetInnerHTML` 未经处理的 HTML） |
| 性能 | 结果页渲染 < 1s；转写、总结期间页面不卡死（异步处理） |
| 兼容 | 支持现代 Chrome / Edge / Safari / Firefox；Pake 打包的 WebView 环境可用（无 Node API、无特殊系统权限依赖） |
| 体积 | 构建产物尽量小，不引入重型依赖 |

---

## 4. 技术方案建议

### 4.1 技术栈

| 层 | 选型 | 说明 |
| --- | --- | --- |
| 框架 | Vite + React + TypeScript（备选：Vue 3） | 生态成熟，构建快 |
| Markdown 渲染 | react-markdown（或 marked）+ DOMPurify | 渲染后过滤，防 XSS |
| 复制 | `navigator.clipboard.writeText` | 浏览器原生，无需库 |
| 状态管理 | zustand 或 React Context | 管理配置、处理状态、结果 |
| 样式 | 原生 CSS / Tailwind 均可 | 卡片式布局为主 |
| UI 组件 | 可选（antd 等），非必须 | 保持轻量 |

### 4.2 架构示意

```
┌─────────────────────────────────────────────────┐
│              浏览器（SPA，无后端）                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────────┐   │
│  │  首页     │  │ 结果页    │  │  设置页       │   │
│  │ 上传音频  │→│ 9 个窗口  │  │ STT/总结配置 │   │
│  └──────────┘  │ Markdown │  └──────────────┘   │
│      │         │ + 复制   │         │            │
│      ▼         └──────────┘         ▼            │
│  ┌─────────────────────────────────────────┐    │
│  │  处理流程：转写 → 总结 → 切分结果          │    │
│  └─────────────────────────────────────────┘    │
│  localStorage：API 配置 / 转写文本 / 总结结果     │
└─────────────────────────────────────────────────┘
        │  fetch 直连（CORS）         │  fetch 直连
        ▼                             ▼
  STT API（Whisper 兼容）        总结 API（Chat Completions 兼容）
```

### 4.3 关键实现要点

- **CORS**：OpenAI、DeepSeek、Groq、SiliconFlow 等主流服务商均允许浏览器跨域直连，纯前端方案可行；若个别服务商不支持，在设置页的服务商预设中标注。
- **总结输出切分**：约定总结模型输出以一级标题（`# 窗口名`）分隔各窗口内容，前端解析 Markdown 按标题切分渲染到对应窗口；未匹配到约定标题的内容归入「完整逐字稿」或忽略并提示。
- **说话人区分**：Whisper 不支持说话人分离，由总结模型根据上下文推断（如"面试官："、"我："前缀）；转写质量不足时模型应注明局限性。
- **Pake 打包**：仅需打包 `dist/` 静态产物；配置存储依赖 localStorage（Tauri WebView 支持持久化），无特殊适配。

---

## 5. 页面与交互流程

### 5.1 页面结构（单页应用，路由切换）

| 页面 | 说明 |
| --- | --- |
| 首页 | Logo + 标题；上传区（拖拽/点击）；面试背景信息表单（可折叠，可选填）；「开始分析」按钮（未配置 Key 时引导去设置） |
| 处理页 | 三步进度条（上传 → 转写 → 总结），每步显示状态、耗时，失败可重试 |
| 结果页 | 顶部：面试背景信息摘要 + 「重新分析」「导出 .md」按钮；下方：9 个窗口卡片（网格/瀑布流布局），每卡片标题栏含「复制」按钮 |
| 设置页 | STT 配置卡片 + 总结配置卡片（各自含测试连接按钮），安全提示文案，可选导出/导入配置 |

### 5.2 主流程时序

```
用户选择音频 → 前端校验格式/大小 → 调 STT API 转写（显示进度②）
→ 转写成功，缓存逐字稿 → 调总结 API（携带逐字稿+背景信息+System Prompt，显示进度③）
→ 解析返回的 Markdown → 切分 9 个窗口 → 渲染结果页 → 用户点击「复制」→ 复制窗口 Markdown 原文
```

---

## 6. 里程碑规划

| 版本 | 范围 | 说明 |
| --- | --- | --- |
| **MVP** | FR-1、FR-2（含分片 FR-2.6）、FR-3、FR-4（窗口 1–9）、FR-5（含测试连接） | 核心链路完整可用：上传 → 转写（超限自动分片）→ 总结 → 展示 + 复制 + 设置 |
| **V1.1** | FR-6 历史记录、导出全部窗口为 .md、Pake 打包验证、配置导出/导入 | 提升易用性与交付形态 |
| **V1.2**（可选） | 说话人分离（接入支持 diarization 的服务商）、音频格式转换（m4a → wav/mp3）、更多服务商预设 | 解决精度与格式兼容问题 |

---

## 7. 风险与待确认项

| # | 风险/待确认 | 影响 | 应对 |
| --- | --- | --- | --- |
| 1 | 音频大小超出服务商限制（如 MiMo Base64 ≤10MB） | 长录音传不上去 | 已实现自动分片（解码 → 切片 → 逐段转写 → 合并）；无法解码的格式提示先转换 |
| 2 | 说话人无法准确分离 | 逐字稿与总结中"谁说的"可能不准 | 模型推断 + 注明局限；V1.2 接入支持 diarization 的服务商 |
| 3 | 长逐字稿超出模型上下文 | 总结截断/失败 | 截断提示；V1.2 分段总结 |
| 4 | 个别服务商不支持浏览器 CORS | 直连失败 | 服务商预设中标注支持情况；备选方案再议 |
| 5 | Markdown 渲染 XSS 风险 | 注入脚本 | DOMPurify 过滤（必做） |
| 6 | 总结一次生成 9 个窗口可能偶尔漏生成某窗口 | 某窗口为空 | 空窗口显示占位；支持"仅重试该窗口"（重新调用并限定只生成该部分） |
| 7 | **待确认**：默认 System Prompt（复盘提示词）的窗口标题约定，需与前端切分逻辑保持一致 | 解析失败 | 提示词与解析器用同一份「窗口标题清单」常量，集中维护 |

---

## 8. 验收标准（MVP）

1. 未配置 API Key 时，首页引导用户前往设置页，配置后返回可继续；
2. 上传一段 ≤25MB 的面试录音（mp3/wav/m4a），自动完成转写与总结，无需刷新；
3. **上传超过接口限制的大文件（如 20MB）时，首页给出「将自动分片」警告并可正常开始分析，转写阶段显示分片进度，结果与整段转写等效；**
3. 结果页展示 9 个窗口，内容为渲染后的 Markdown（含列表/表格/代码块样式）；
4. 点击任一窗口「复制」，剪贴板内容为该窗口 Markdown 原文，粘贴到笔记工具后格式保持；
5. STT 与总结使用不同服务商配置时，各调用各的（验证两组配置相互独立）；
6. 设置页两组配置的「测试连接」均能返回明确的成功/失败结果；
7. 转写或总结失败时，页面显示可理解的错误原因并可重试；
8. `npm run build` 产物可部署到静态托管并完整工作；Pake 打包（V1.1 验证）后核心功能正常。
